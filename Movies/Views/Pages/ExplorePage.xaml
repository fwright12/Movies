<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maui="clr-namespace:Microsoft.Maui.Controls;assembly=MauiExtensions"
             xmlns:ext="clr-namespace:Microsoft.Maui.Controls.Extensions;assembly=MauiExtensions"
             xmlns:movies="clr-namespace:Movies"
             xmlns:strings="clr-namespace:Movies.Resources.Strings"
             xmlns:views="clr-namespace:Movies.Views"
             x:Class="Movies.Views.ExplorePage">
    <Page.ToolbarItems>
        <ToolbarItem Text="{Static strings:AppResources.SettingsToolbar}">
            <ToolbarItem.Command>
                <views:PushPageCommand PageTemplate="{DataTemplate views:SettingsPage}" />
            </ToolbarItem.Command>
        </ToolbarItem>
    </Page.ToolbarItems>

    <CollectionView ItemsSource="{Binding}"
                    ItemTemplate="{StaticResource CondensedListTemplate}"
                    ItemsUpdatingScrollMode="KeepScrollOffset"
                    Header="test"
                    Footer="test">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical"
                               ext:ItemsLayout.ItemSize="250" />
        </CollectionView.ItemsLayout>

        <CollectionView.HeaderTemplate>
            <DataTemplate>
                <ContentView>
                    <maui:AdView Style="{StaticResource BannerAdStyle}"
                                 AdUnitID="{Static movies:App.TopBannerAdUnitID}" />
                </ContentView>
            </DataTemplate>
        </CollectionView.HeaderTemplate>

        <CollectionView.FooterTemplate>
            <DataTemplate>
                <ContentView>
                    <maui:AdView Style="{StaticResource BannerAdStyle}"
                                 AdUnitID="{Static movies:App.BottomBannerAdUnitID}" />
                </ContentView>
            </DataTemplate>
        </CollectionView.FooterTemplate>

        <maui:Ads.CollectionAds>
            <maui:AdInfo Frequency="3"
                         Template="{StaticResource InlineBannerTemplate}" />
        </maui:Ads.CollectionAds>
    </CollectionView>
</ContentPage>